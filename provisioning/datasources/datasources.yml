apiVersion: 1

deleteDatasources:
  - name: Prometheus
    orgId: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    password:
    user:
    database:
    basicAuth: false
    basicAuthUser:
    basicAuthPassword:
    withCredentials:
    isDefault: true
    jsonData:
      graphiteVersion: "1.1"
      tlsAuth: false
      tlsAuthWithCACert: false
    secureJsonData:
      tlsCACert: "..."
      tlsClientCert: "..."
      tlsClientKey: "..."
    version: 1
    editable: true
    uid: t4f0shgRk

  - name: Loki
    type: loki
    access: proxy
    orgId: 1
    url: http://loki:3100
    password:
    user:
    database:
    basicAuth: false
    basicAuthUser:
    basicAuthPassword:
    withCredentials:
    isDefault: false
    jsonData:
      maxLines: 1000
      derivedFields:
        - datasourceName: Prometheus
          matcherRegex: "container=\"([^\"]+)\""
          name: Container metrics
          datasourceUid: t4f0shgRk
          url: '/explore?orgId=1&left=["now-1h","now","Prometheus",{"expr":"container_name=\"$${__value.raw}\""}]'
    secureJsonData: {}
    version: 1
    editable: true
    uid: loki-datasource
