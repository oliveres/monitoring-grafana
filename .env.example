# Central Monitoring Stack - Environment Variables
# Copy this file to .env and update with your values

# Domain Configuration (Required)
# Your domain must point to this server's public IP for SSL to work
DOMAIN=monitoring.example.com

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-this-secure-password

# Prometheus Configuration
# Retention period for metrics (default: 2y = 2 years)
PROMETHEUS_RETENTION=2y

# Basic Authentication (For Remote VPS Access)
# Generate hash with: htpasswd -nbB remote your-password
# Use only the hash part (everything after "remote:")
BASIC_AUTH_USER=remote
BASIC_AUTH_PASSWORD_HASH=$2a$12$replace-with-your-bcrypt-hash

# Optional: VPC Configuration (For DigitalOcean)
# Document your VPC private IP here for reference
# VPC_PRIVATE_IP=10.116.0.2
